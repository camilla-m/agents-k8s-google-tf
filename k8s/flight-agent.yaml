apiVersion: apps/v1
kind: Deployment
metadata:
  name: flight-agent
  namespace: adk-travel
  labels:
    app: flight-agent
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flight-agent
  template:
    metadata:
      labels:
        app: flight-agent
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8090"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: adk-agents
      containers:
      - name: flight-agent
        image: gcr.io/PROJECT_ID/flight-agent:latest
        ports:
        - containerPort: 8080
          name: http
        - containerPort: 8090
          name: metrics
        env:
        - name: AGENT_TYPE
          value: "flight"
        - name: METRICS_PORT
          value: "8090"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: flight-agent
  namespace: adk-travel
  labels:
    app: flight-agent
spec:
  selector:
    app: flight-agent
  ports:
  - port: 80
    targetPort: 8080
    name: http
  - port: 8090
    targetPort: 8090
    name: metrics
  type: ClusterIP